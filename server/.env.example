# Server
PORT=4000
NODE_ENV=development

# Auth
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# CORS (comma-separated origins). Leave empty to allow all origins (development only)
CORS_ORIGINS=

# Stripe (optional)
STRIPE_SECRET_KEY=

